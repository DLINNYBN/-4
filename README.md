#include <iostream>
#include <locale.h>
using namespace std;

int main()
{
    setlocale(LC_ALL, "Rus");

    int a,
    cout << "Введите значение а: ";
    cin >> a;
  
    cout << " Значение а = " << c << endl;
    cout << "Всё хорошо\ ";
    
}
